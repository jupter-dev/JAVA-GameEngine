package Entities.Collectables;

import java.awt.image.BufferedImage;

import Entities.Entity;

public class Gem extends Entity {

    public Gem(int x, int y, int w, int h, BufferedImage sprite) {
        super(x, y, w, h, sprite);
    }
    
}
